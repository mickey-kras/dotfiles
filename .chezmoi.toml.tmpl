{{- /* Chezmoi config — runs during `chezmoi init`. Re-run init to change. */ -}}

{{- $enableApiMcps := promptBoolOnce . "enable_api_mcps" "Enable API-key MCPs (exa, firecrawl, fal-ai)? Requires Bitwarden CLI" -}}

[data]
  enable_api_mcps = {{ $enableApiMcps }}
